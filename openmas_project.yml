name: openmas
version: 0.0.1
agents:
  example: examples/00_hello_agent
extension_paths:
  - extensions/
shared_paths:
  - shared/
default_config:
  name: default_agent
  communicator_type: mock
  service_urls: {}
