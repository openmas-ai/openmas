version: "1.0"

# Define the components to orchestrate
components:
  - name: agent1
    path: agent1/openmas.deploy.yaml

  - name: agent2
    path: agent2/openmas.deploy.yaml

# Global configuration (applied to all components)
global:
  # Shared network for the agents
  networks:
    - name: agent-network
      driver: bridge
